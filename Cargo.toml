[package]
name = "syscallsnoop"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
redhook = "2.0"

[lib]
crate_type = ["dylib"]
